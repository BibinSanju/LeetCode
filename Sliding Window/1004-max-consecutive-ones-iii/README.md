Idea is dynamic sliding window, r moves through every elemnet, i keep track of zero count, when i exceeds k , i shrink the window till the z_count comes == k , when window grow, i calc max...

TC = O(N)
SC = O(1)
